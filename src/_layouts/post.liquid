---
layout: default
---

<h1>{{ page.title }}</h1>

<img src="{{ page.thumbnail }}" alt="{{ page.title }}" class="rounded h-60 w-full object-cover" />

<p class="text-sm">:written_by => {{ page.author }}</p>

{{ content }}

<!-- if disable comments true then hide comments comments -->
### comments will go here
